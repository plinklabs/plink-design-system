using Microsoft.UI.Xaml;

namespace PlinkDesignSystem.Sample;

/// <summary>The verify window — a gallery of the binding: the ink section, the
/// type ramp, buttons (primary spark + neutral), an input, a card, and Ping.</summary>
public sealed partial class MainWindow : Window
{
    public MainWindow() => InitializeComponent();
}
