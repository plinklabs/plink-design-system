Hairline text input / textarea with a magenta focus ring and optional mono label.

```jsx
<Input label="Email" type="email" placeholder="you@school.edu" />
<Input multiline label="Notes" rows={4} />
```

Props: `label`, `multiline`, `invalid`.
