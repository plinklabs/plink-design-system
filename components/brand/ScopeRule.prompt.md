A thin oscilloscope waveform line used as a recurring rule — a flat signal that plinks once.

```jsx
<ScopeRule position={0.35} />
<ScopeRule onInk position={0.6} />
```

`position` (0–1) places the plink; `onInk` recolours for dark sections.
