The Plink Labs logo: open-ring mark with optional "plink labs" wordmark.

```jsx
<Logo size={28} />
<Logo variant="mark" />
<Logo theme="dark" size={32} />   {/* on the full-bleed ink section */}
```

`variant`: `full` | `mark`. `theme`: `light` | `dark`. `size` is the mark height in px.
