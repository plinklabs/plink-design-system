Section divider — a ping radiating across a hairline rule. Use between major sections.

```jsx
<PingDivider position={0.33} />
<PingDivider onInk />
```

`position` (0–1) slides the ping for editorial asymmetry. Respects `prefers-reduced-motion`.
