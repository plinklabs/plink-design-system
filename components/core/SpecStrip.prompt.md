A row of mono technical specs split by hairline rules — the "Labs" microcopy signature.

```jsx
<SpecStrip items={["GPL-3.0", "GDPR-clean", "Works offline", "16px → projector"]} />
```

`onInk` switches colours for a dark background.
